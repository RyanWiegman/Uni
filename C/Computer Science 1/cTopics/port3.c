// Check if temperature is hot 

#include <stdio.h> 

  

int main(void) 

{ 

    int unit, temp; 

  

    // printf(...); 

    scanf("%d%d", &unit, &temp); 

  

    if((unit == 0 && temp >= 80) || (unit == 1 && temp >= 80)) 

        // printf(.....); 

} 
